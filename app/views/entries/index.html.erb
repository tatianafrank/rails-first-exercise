
<h1> <%= @project.name %></h1>
<ul>
	<% @entries.each do |entry| %>
	<li>
		<%= entry.hours %> h
		<%= entry.minutes %> -m
		<%= entry.date %> 
	</li>
	<% end %>
</ul>


<%= @project.total_hours_in_month(DateTime.now.month,DateTime.now.year) %>